// Swamp, an Automation Framework
// Copyright (C) 2026 Elder Swamp Club, Inc.
//
// This file is part of Swamp.
//
// Swamp is free software: you can redistribute it and/or modify
// it under the terms of the GNU Affero General Public License version 3
// as published by the Free Software Foundation, with the Swamp
// Extension and Definition Exception (found in the "COPYING-EXCEPTION"
// file).
//
// Swamp is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU Affero General Public License for more details.
//
// You should have received a copy of the GNU Affero General Public License
// along with Swamp.  If not, see <https://www.gnu.org/licenses/>.

// Auto-generated extension model for @swamp/aws/appconfig/experiment-definition
// Do not edit manually. Re-generate with: deno task generate:aws

// deno-lint-ignore-file no-explicit-any

/**
 * Swamp extension model for AppConfig ExperimentDefinition (AWS::AppConfig::ExperimentDefinition).
 *
 * Wraps the CloudFormation resource type as a swamp model so create,
 * get, update, delete, and sync can be driven through `swamp model`.
 *
 * @module
 */

import { z } from "npm:zod@4.3.6";
import {
  createResource,
  deleteResource,
  isResourceNotFoundError,
  readResource,
  updateResource,
} from "./_lib/aws.ts";
import type { AwsCredentials } from "./_lib/aws.ts";

const AttributeValueSchema = z.object({
  StringArray: z.array(z.string()).describe("An array of string values.")
    .optional(),
  NumberValue: z.number().describe("A numeric value.").optional(),
  BooleanValue: z.boolean().describe("A boolean value.").optional(),
  StringValue: z.string().describe("A string value.").optional(),
  NumberArray: z.array(z.number()).describe("An array of numeric values.")
    .optional(),
});

const TreatmentSchema = z.object({
  Description: z.string().max(1024).optional(),
  AttributeValues: z.record(z.string(), AttributeValueSchema).describe(
    "Map of attribute name to attribute value.",
  ).optional(),
  Enabled: z.boolean().describe(
    "Whether the flag is enabled for this variant.",
  ),
  Key: z.string().describe(
    "The treatment key (read-only, auto-generated by service).",
  ).optional(),
  Weight: z.number().describe("Traffic weight percentage."),
});

const TagSchema = z.object({
  Value: z.string().min(0).max(256).describe("The tag value."),
  Key: z.string().min(1).max(128).describe("The tag key."),
});

const GlobalArgsSchema = z.object({
  name: z.string().describe(
    "Instance name for this resource (used as the unique identifier in the factory pattern)",
  ),
  accessKeyId: z.string().meta({ sensitive: true }).describe(
    "AWS access key ID; overrides AWS_ACCESS_KEY_ID environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  secretAccessKey: z.string().meta({ sensitive: true }).describe(
    "AWS secret access key; overrides AWS_SECRET_ACCESS_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  sessionToken: z.string().meta({ sensitive: true }).describe(
    "AWS session token for temporary credentials; overrides AWS_SESSION_TOKEN environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  region: z.string().describe(
    "AWS region; overrides AWS_REGION / AWS_DEFAULT_REGION environment variables and ~/.aws/config profile region. Defaults to us-east-1.",
  ).optional(),
  EnvironmentIdentifier: z.string().min(1).max(2048).describe(
    "The environment ID, name, or ARN.",
  ),
  AudienceDescription: z.string().max(1024).describe(
    "Human-readable description of the audience.",
  ).optional(),
  LaunchCriteria: z.string().max(1024).describe(
    "Criteria for launching the experiment.",
  ).optional(),
  ApplicationIdentifier: z.string().min(1).max(2048).describe(
    "The application ID, name, or ARN.",
  ),
  AudienceRule: z.string().max(16384).describe(
    "Rule expression defining the experiment audience.",
  ),
  Hypothesis: z.string().max(1024).describe("The hypothesis of the experiment.")
    .optional(),
  Name: z.string().min(1).max(64).describe(
    "A name for the experiment definition.",
  ),
  ConfigurationProfileIdentifier: z.string().min(1).max(2048).describe(
    "The configuration profile ID, name, or ARN.",
  ),
  Control: z.object({
    Description: z.string().max(1024).optional(),
    AttributeValues: z.record(z.string(), AttributeValueSchema).describe(
      "Map of attribute name to attribute value.",
    ).optional(),
    Enabled: z.boolean().describe(
      "Whether the flag is enabled for this variant.",
    ),
    Weight: z.number().describe("Traffic weight percentage."),
  }).describe("The control (baseline) variant."),
  FlagKey: z.string().min(1).max(64).describe(
    "The key of the existing flag in the configuration profile to experiment on.",
  ),
  Treatments: z.array(TreatmentSchema).describe("Treatment variants (1-5)."),
  Tags: z.array(TagSchema).describe(
    "Tags to associate with the experiment definition.",
  ).optional(),
});

const StateSchema = z.object({
  Status: z.string().optional(),
  EnvironmentIdentifier: z.string().optional(),
  AudienceDescription: z.string().optional(),
  CreatedAt: z.string().optional(),
  LaunchCriteria: z.string().optional(),
  ApplicationIdentifier: z.string().optional(),
  AudienceRule: z.string().optional(),
  UpdatedAt: z.string().optional(),
  Hypothesis: z.string().optional(),
  Name: z.string().optional(),
  ConfigurationProfileIdentifier: z.string().optional(),
  Control: TreatmentSchema.optional(),
  FlagKey: z.string().optional(),
  Id: z.string(),
  ApplicationId: z.string(),
  Treatments: z.array(TreatmentSchema).optional(),
  Tags: z.array(TagSchema).optional(),
}).passthrough();

type StateData = z.infer<typeof StateSchema>;

const InputsSchema = z.object({
  name: z.string().optional(),
  accessKeyId: z.string().meta({ sensitive: true }).optional(),
  secretAccessKey: z.string().meta({ sensitive: true }).optional(),
  sessionToken: z.string().meta({ sensitive: true }).optional(),
  region: z.string().optional(),
  EnvironmentIdentifier: z.string().min(1).max(2048).describe(
    "The environment ID, name, or ARN.",
  ).optional(),
  AudienceDescription: z.string().max(1024).describe(
    "Human-readable description of the audience.",
  ).optional(),
  LaunchCriteria: z.string().max(1024).describe(
    "Criteria for launching the experiment.",
  ).optional(),
  ApplicationIdentifier: z.string().min(1).max(2048).describe(
    "The application ID, name, or ARN.",
  ).optional(),
  AudienceRule: z.string().max(16384).describe(
    "Rule expression defining the experiment audience.",
  ).optional(),
  Hypothesis: z.string().max(1024).describe("The hypothesis of the experiment.")
    .optional(),
  Name: z.string().min(1).max(64).describe(
    "A name for the experiment definition.",
  ).optional(),
  ConfigurationProfileIdentifier: z.string().min(1).max(2048).describe(
    "The configuration profile ID, name, or ARN.",
  ).optional(),
  Control: z.object({
    Description: z.string().max(1024).optional(),
    AttributeValues: z.record(z.string(), AttributeValueSchema).describe(
      "Map of attribute name to attribute value.",
    ).optional(),
    Enabled: z.boolean().describe(
      "Whether the flag is enabled for this variant.",
    ).optional(),
    Weight: z.number().describe("Traffic weight percentage.").optional(),
  }).describe("The control (baseline) variant.").optional(),
  FlagKey: z.string().min(1).max(64).describe(
    "The key of the existing flag in the configuration profile to experiment on.",
  ).optional(),
  Treatments: z.array(TreatmentSchema).describe("Treatment variants (1-5).")
    .optional(),
  Tags: z.array(TagSchema).describe(
    "Tags to associate with the experiment definition.",
  ).optional(),
});

const _credentialKeys = new Set([
  "accessKeyId",
  "secretAccessKey",
  "sessionToken",
  "region",
]);

function _buildCredentials(g: Record<string, unknown>): AwsCredentials {
  return {
    accessKeyId: g.accessKeyId as string | undefined,
    secretAccessKey: g.secretAccessKey as string | undefined,
    sessionToken: g.sessionToken as string | undefined,
    region: g.region as string | undefined,
  };
}

/** Swamp extension model for AppConfig ExperimentDefinition. Registered at `@swamp/aws/appconfig/experiment-definition`. */
export const model = {
  type: "@swamp/aws/appconfig/experiment-definition",
  version: "2026.08.07.1",
  globalArguments: GlobalArgsSchema,
  inputsSchema: InputsSchema,
  resources: {
    state: {
      description: "AppConfig ExperimentDefinition resource state",
      schema: StateSchema,
      lifetime: "infinite",
      garbageCollection: 10,
    },
  },
  methods: {
    create: {
      description: "Create a AppConfig ExperimentDefinition",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const credentials = _buildCredentials(g);
        const desiredState: Record<string, unknown> = {};
        for (const [key, value] of Object.entries(g)) {
          if (key === "name") continue;
          if (_credentialKeys.has(key)) continue;
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await createResource(
          "AWS::AppConfig::ExperimentDefinition",
          desiredState,
          credentials,
        ) as StateData;
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./g, "_").replace(/\0/g, "");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get a AppConfig ExperimentDefinition",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the AppConfig ExperimentDefinition",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const credentials = _buildCredentials(context.globalArgs);
        const result = await readResource(
          "AWS::AppConfig::ExperimentDefinition",
          args.identifier,
          credentials,
        ) as StateData;
        const instanceName =
          (context.globalArgs.name?.toString() ?? args.identifier).replace(
            /[\/\\]/g,
            "_",
          ).replace(/\.\./g, "_").replace(/\0/g, "");
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update a AppConfig ExperimentDefinition",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const credentials = _buildCredentials(g);
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./g, "_").replace(/\0/g, "");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const idParts = [
          existing.ApplicationId?.toString(),
          existing.Id?.toString(),
        ];
        if (idParts.some((p) => !p)) {
          throw new Error(
            "Missing primary identifier fields in existing state",
          );
        }
        const identifier = idParts.join("|");
        const currentState = await readResource(
          "AWS::AppConfig::ExperimentDefinition",
          identifier,
          credentials,
        ) as StateData;
        const desiredState: Record<string, unknown> = { ...currentState };
        for (const [key, value] of Object.entries(g)) {
          if (key === "name") continue;
          if (_credentialKeys.has(key)) continue;
          if (value !== undefined) desiredState[key] = value;
        }
        const result = await updateResource(
          "AWS::AppConfig::ExperimentDefinition",
          identifier,
          currentState,
          desiredState,
          [
            "ApplicationIdentifier",
            "Name",
            "ConfigurationProfileIdentifier",
            "EnvironmentIdentifier",
            "FlagKey",
          ],
          credentials,
        );
        const handle = await context.writeResource(
          "state",
          instanceName,
          result,
        );
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete a AppConfig ExperimentDefinition",
      arguments: z.object({
        identifier: z.string().describe(
          "The primary identifier of the AppConfig ExperimentDefinition",
        ),
      }),
      execute: async (args: { identifier: string }, context: any) => {
        const credentials = _buildCredentials(context.globalArgs);
        const { existed } = await deleteResource(
          "AWS::AppConfig::ExperimentDefinition",
          args.identifier,
          credentials,
        );
        const instanceName =
          (context.globalArgs.name?.toString() ?? args.identifier).replace(
            /[\/\\]/g,
            "_",
          ).replace(/\.\./g, "_").replace(/\0/g, "");
        const handle = await context.writeResource("state", instanceName, {
          identifier: args.identifier,
          existed,
          status: existed ? "deleted" : "not_found",
          deletedAt: new Date().toISOString(),
        });
        return { dataHandles: [handle] };
      },
    },
    sync: {
      description: "Sync AppConfig ExperimentDefinition state from AWS",
      arguments: z.object({}),
      execute: async (_args: Record<string, never>, context: any) => {
        const g = context.globalArgs;
        const credentials = _buildCredentials(g);
        const instanceName = (g.name?.toString() ?? "current").replace(
          /[\/\\]/g,
          "_",
        ).replace(/\.\./g, "_").replace(/\0/g, "");
        const content = await context.dataRepository.getContent(
          context.modelType,
          context.modelId,
          instanceName,
        );
        if (!content) {
          throw new Error("No existing state found - run create or get first");
        }
        const existing = JSON.parse(new TextDecoder().decode(content));
        const idParts = [
          existing.ApplicationId?.toString(),
          existing.Id?.toString(),
        ];
        if (idParts.some((p) => !p)) {
          throw new Error(
            "Missing primary identifier fields in existing state",
          );
        }
        const identifier = idParts.join("|");
        try {
          const result = await readResource(
            "AWS::AppConfig::ExperimentDefinition",
            identifier,
            credentials,
          ) as StateData;
          const handle = await context.writeResource(
            "state",
            instanceName,
            result,
          );
          return { dataHandles: [handle] };
        } catch (error: unknown) {
          if (isResourceNotFoundError(error)) {
            const handle = await context.writeResource("state", instanceName, {
              identifier,
              status: "not_found",
              syncedAt: new Date().toISOString(),
            });
            return { dataHandles: [handle] };
          }
          throw error;
        }
      },
    },
  },
};
